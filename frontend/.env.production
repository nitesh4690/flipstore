# Baked into production builds (loaded by Vite when mode=production, which is
# what Vercel's `npm run build` uses). Local dev (mode=development) ignores this
# file and keeps using the /api proxy from vite.config.js.
VITE_API_URL=https://flipstore-backend-tan.vercel.app
